{% extends "iseaAppBundle::Layout/backoffice_layout.html.twig" %}

{% block body -%}
    <h1>Article edit</h1>
	{{ form_start(edit_form, {'attr': {'class': 'backofficeForm'}}) }}
		{{ form(edit_form) }}
	{{ form_end(edit_form) }}

        <ul class="record_actions">
    <li>
        <a class="button button-red" href="{{ path('backoffice_article') }}">
            Retour à la liste
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
